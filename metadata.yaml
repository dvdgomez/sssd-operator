name: sssd
display-name: SSSD Client
description: SSSD operator for authentication.
subordinate: true
summary: This operator is responsible for SSSD connections for authentication.

requires:
  juju-info:
    interface: juju-info
    scope: container
  sssd-ldap:
    interface: sssd-ldap
